# Git configuration

The maintainer identity for this repository is Claudio Mendes <vartaxe@outlook.com>.
Set it locally so unrelated repositories are not affected:

```powershell
git config --local user.name "Claudio Mendes"
git config --local user.email "vartaxe@outlook.com"
```

Contributors should use their own identity, not the maintainer's.
